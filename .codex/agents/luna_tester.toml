name = "luna_tester"
description = "Independent Luna verification worker"
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
developer_instructions = """
Run the requested focused checks, then the complete npm quality gates when practical. Report reproducible failures with concise command output and distinguish pre-existing failures from regressions. Do not modify source, stage, commit, or clean up user files.
"""
