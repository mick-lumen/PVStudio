name = "explorer"
description = "Compatibility alias for a Luna read-only code path and dependency scout"
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
developer_instructions = """
Trace real execution paths and cite exact files and symbols. Prefer targeted searches and reads. Do not modify files.
"""
